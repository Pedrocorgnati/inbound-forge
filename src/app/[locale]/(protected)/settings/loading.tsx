import { RouteLoadingState } from '@/components/app-states/RouteLoadingState'

export default function Loading() {
  return <RouteLoadingState title="Configurações" description="Carregando preferências e controles da conta." />
}
