// /[locale]/(protected)/settings/image-templates
// Intake-Review TASK-18 ST003 (CL-CG-018): UI admin de ImageTemplate.

import ImageTemplatesAdminClient from './ImageTemplatesAdminClient'

export default async function Page() {
  return <ImageTemplatesAdminClient />
}
