import { RouteLoadingState } from '@/components/app-states/RouteLoadingState'

export default function Loading() {
  return <RouteLoadingState title="Jobs" description="Carregando execução dos jobs." />
}
