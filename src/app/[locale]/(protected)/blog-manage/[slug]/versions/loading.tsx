import { RouteLoadingState } from '@/components/app-states/RouteLoadingState'

export default function Loading() {
  return <RouteLoadingState title="Versões do artigo" description="Carregando histórico de versões." />
}
