import { RouteLoadingState } from '@/components/app-states/RouteLoadingState'

export default function Loading() {
  return <RouteLoadingState title="Auditoria de compliance" description="Carregando eventos auditáveis." />
}
