import { RouteLoadingState } from '@/components/app-states/RouteLoadingState'

export default function Loading() {
  return <RouteLoadingState title="Fontes" description="Carregando fontes monitoradas." />
}
