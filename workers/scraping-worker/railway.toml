[build]
builder = "DOCKERFILE"
dockerfilePath = "workers/scraping-worker/Dockerfile"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "scraping-worker"
